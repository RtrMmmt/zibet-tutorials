# ----------------------------------------------------------------
# environment
CC      = mpicc
FC      = 

# ----------------------------------------------------------------
# options
CFLAGS  = -fopenmp
FFLAGS  = 

# ----------------------------------------------------------------
# sources and objects
C_SRC   = ex3.c
F_SRC   = 

C_OBJ   = $(C_SRC:.c=.o)
F_OBJ   = $(F_SRC:.f=.o)

# ----------------------------------------------------------------
# executables
EXEC    = ex3

all:    $(EXEC)

$(EXEC): $(C_OBJ)
	$(CC) $(CFLAGS) -o $@ $(C_OBJ)

# ----------------------------------------------------------------
# rules
.c.o:
	$(CC) $(CFLAGS) -c $< -o $@

.f.o:
	$(FC) $(FFLAGS) -c $< -o $@

# ----------------------------------------------------------------
# clean up
clean:
	rm -f $(EXEC) $(C_OBJ) $(F_OBJ)

# ----------------------------------------------------------------
# End of Makefile
